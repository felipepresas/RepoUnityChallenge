using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AnimationController : MonoBehaviour
{
  //  private Animator _animator;
   // private const string MOVE_HANDS = "Move hand"; //Se crea variable con el nombre del animator 
    //private bool isMovingHands = false; //Se crea tipo de variable para activar el animator

       void Start()
    {
       // _animator = GetComponent<Animator>();   //se accede al animator
      //  _animator.SetBool(name: MOVE_HANDS, isMovingHands);  // Se le asigna el valor falso de la variable al inicializarla
    }

    void Update()
    {
     //   if (Input.GetKeyDown(KeyCode.Space))
      //  {
      //      isMovingHands = !isMovingHands;   //le cambia el valor movinghands
     //       _animator.SetBool(name: MOVE_HANDS, isMovingHands); // se le modifica el valor sengun se ejecute condicional
       // }
    }
}
